# ============================================================================
# Deepfake-Resistant Provenance & Verification System - Production Environment
# Copy this file to .env.production on your server and replace placeholders.
# DO NOT COMMIT .env.production WITH REAL CREDENTIALS.
# ============================================================================

# Database Configuration (PostgreSQL 15)
POSTGRES_USER=provenance
POSTGRES_PASSWORD=generate-strong-random-password-here
POSTGRES_DB=provenance_db
DATABASE_URL=postgresql://provenance:generate-strong-random-password-here@postgres:5432/provenance_db

# Redis Infrastructure
REDIS_URL=redis://redis:6379/0

# Security & JWT Credentials
SECRET_KEY=generate-at-least-32-chars-cryptographic-random-secret
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# Google OAuth (For Google Sign-In)
GOOGLE_CLIENT_ID=your-google-oauth-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-oauth-client-secret
GOOGLE_REDIRECT_URI=https://your-domain.com/api/v1/auth/google/callback

# Meta WhatsApp Cloud API Credentials
WHATSAPP_APP_ID=your-meta-app-id
WHATSAPP_APP_SECRET=your-meta-app-secret
WHATSAPP_PHONE_NUMBER_ID=your-whatsapp-business-phone-number-id
WHATSAPP_ACCESS_TOKEN=your-meta-system-user-or-permanent-access-token
WHATSAPP_VERIFY_TOKEN=your-custom-webhook-verify-token-string

# Storage and Limits
MAX_UPLOAD_SIZE=16777216
MEDIA_PROCESSING_TIMEOUT_SECONDS=120
ALLOWED_EXTENSIONS=mp4,avi,mov,mp3,wav,jpg,png,pdf,txt
UPLOAD_DIR=uploads
TEMP_DIR=uploads/temp
PROCESSED_DIR=uploads/processed

# CORS and Domains (Comma-separated list or JSON array of allowed origins)
BACKEND_CORS_ORIGINS=["https://your-domain.com","https://api.your-domain.com"]
API_V1_PREFIX=/api/v1

# Frontend Configuration
NEXT_PUBLIC_API_URL=https://your-domain.com/api/v1
